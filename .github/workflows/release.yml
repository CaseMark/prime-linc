name: Release prime-linc

on:
  workflow_dispatch:

permissions: {}

concurrency:
  group: release-prime-linc
  cancel-in-progress: false

jobs:
  release:
    name: Tag reviewed main and start release
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
    steps:
      - name: Checkout main
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: main
          fetch-depth: 0

      - name: Resolve version
        id: version
        run: |
          VERSION="$(node -p "require('./packages/coding-agent/package.json').version")"
          TAG="v${VERSION}"
          if git rev-parse "refs/tags/${TAG}" >/dev/null 2>&1; then
            echo "${TAG} already exists; bump the version in a reviewed PR first." >&2
            exit 1
          fi
          echo "tag=${TAG}" >> "${GITHUB_OUTPUT}"

      - name: Tag reviewed main
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${TAG}" -m "Release ${TAG}"
          git push origin "${TAG}"

      # Pushes made with GITHUB_TOKEN intentionally do not trigger push
      # workflows. Dispatch github-release.yml explicitly from reviewed main.
      - name: Start isolated release publisher
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        run: gh workflow run github-release.yml --repo "${GITHUB_REPOSITORY}" --ref main -f "tag=${TAG}"

      - name: Summary
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          {
            echo "## Release started"
            echo
            echo "- Tagged reviewed main as ${TAG}."
            echo "- Dispatched the isolated GitHub Release workflow."
            echo "- Publication waits at the github-release environment gate."
          } >> "${GITHUB_STEP_SUMMARY}"
